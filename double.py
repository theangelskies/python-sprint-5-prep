def double(number):
    return number * 2

print(double(10))

# ✍️exercise
# Read the above code and write down what the bug is. How would you fix it?
# The bug is that the function is named "double" but it actually triples the input number. To fix this, I would change the multiplication factor from 3 to 2, so the function correctly doubles the input number.